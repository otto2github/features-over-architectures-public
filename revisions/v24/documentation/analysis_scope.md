# Reported analyses and inferential scope

No model was fitted and no private-score bootstrap was executed while preparing v23. Empirical numbers are preserved from the supplied and previously executed results. Algebraic positive-complement AUCs and zero-yield consequences are reproducible from those aggregates.

## Main-table intervals

Only crossed company-and-matched-seed intervals are displayed in the main text (Tables 4 and 5). Tables 2A and 3 are descriptive: their existing fixed-fit condition/feature intervals remain available in the Supplement and source CSVs. No new crossed versions of the Table 2A/3 intervals are claimed. This reporting choice is not a recalculation of inference.

## Analyses outside the reported scope

Exact positive-composition reference enumeration; stored-versus-reverse prediction evaluation on D/S/B; primary-matrix nonfinite/source-absence contrasts; a company-versus-document row-level cross-tabulation; year-specific negative source-absence counts; linked investigation-notice histories; and analysis of potentially qualifying sanctions or listing age in anchor-missing rows are not supplied. Neither are new case-blocked, missingness-only, missingness-neutral, joint temporal or rolling-origin fits. Unexecuted evaluators are not included among the paper's evidence.

AUC values on D/O/S/B/J are descriptive, not tests that the selected composition is unusual. D/O source metadata are retrospective and do not establish new-case status. The joint screen contains two positives, one with unresolved financial binding.


The reverse-message configuration is the design-aligned graph reference in the main learner comparison; the stored-message deviation remains a post-hoc ablation. The four GNN M11 display rows use the existing unrounded per-fit aggregate, with no new score-level computation. No positive-composition distribution, company-by-document joint count, validation-side D/O split, or new training result was supplied for v23. Matched seed identifiers and original empirical CSVs remain unchanged.
